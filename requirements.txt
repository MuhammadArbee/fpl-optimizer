requests>=2.31
pandas>=2.1
numpy>=1.26
ortools>=9.10
# pinned exactly (not >=): data/model/linear_model.joblib was saved with these
# versions, and joblib-pickled sklearn models aren't guaranteed to load across
# versions. Re-run `fpl train` after bumping either, then update these pins.
scikit-learn==1.9.1
joblib==1.6.0
streamlit>=1.38
pytest>=8.0
